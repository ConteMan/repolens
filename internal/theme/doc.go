// Package theme holds the built-in html/template set (layout, page, dirlist,
// tree) and static assets (CSS, enhancement JS, vendored mermaid.js), all
// embedded via go:embed. Customization is three-tiered: CSS variables,
// appended CSS, template override. Template names are public API — add
// sparingly.
package theme
